# liblexkey

Encode multiple values into lexicographically sortable keys, in C. Port of [`index-encoder`](https://github.com/holepunchto/index-encoder). See `include/lexkey.h` for the API.

## AI

Note that this is co-authored with AI.

## License

Apache-2.0
